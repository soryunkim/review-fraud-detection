# Progress Report — 2026-09-20

**독립심화학습 I** · 팀원: 김소륜·오동진·김민섭 · 지도교수: 김민경 교수님

> 9/12 §10.3에서 예고만 되고 미배정으로 남아있던 마지막 항목 — **같은 날 동률 엣지 민감도(엄격 제거 / 하루 배치 양방향)** — 를 구현·실행했다. 이로써 미팅자료 3부의 "9/14 이후 신규 목록"이 모두 소진됐다(experiment_matrix.md 정리, 리뷰무작위·작성자단위 seed 반복 등 우선순위 낮은 항목만 남음).

---

## 1. 배경

지금까지 그래프 엣지는 `past_only_adjacency`가 **(date, review_id) 사전식 순서**로 "먼저 쓴 리뷰 → 나중 리뷰" 방향을 정했다. 같은 날 쓴 리뷰끼리는 실제 작성 시각이 아니라 `review_id`(CSV 행 순서)로 순서가 갈리는데(9/12 §10.2 첫날 동률 문제와 같은 근본 원인), 이게 엣지 방향 자체에도 영향을 준다 — R-U-R 엣지의 11.8%(41,375개)가 같은 날 쌍이다.

이번 작업은 이 불확실성을 엣지 구성 단계에서 직접 다루는 두 가지 대안을 구현해 헤드라인 결과(비저활동 R-U-R, 시간단위, +0.050)가 이 선택에 얼마나 민감한지 확인한 것이다. (참고: 9/12의 `--drop-sameday-ties`는 대상 **리뷰**를 빼는 방식이었고, 이번 것은 **엣지 자체**를 바꾸는 방식이라 서로 다른 질문에 답한다.)

---

## 2. 구현

`src/features/structural/temporal_graph.py`의 `past_only_adjacency()`에 `sameday_mode` 파라미터를 추가했다.

- **asof**(기본, 기존과 완전히 동일): `review_id` 순서를 실제 순서처럼 써서 과거 방향 엣지 하나만 만든다.
- **strict**: 같은 날 엣지를 아예 만들지 않는다(날짜가 다를 때만 연결) — "확실히 먼저 쓴 것"만 인정하는 가장 보수적인 버전.
- **bidirectional**: 날짜가 다른 쌍은 asof와 동일(과거→나중 단방향). 같은 날 쌍은 순서를 모른다고 보고 **양방향 모두** 연결한다.

`06_build_graphs_2014.py`에 `--sameday-mode` CLI를 추가해 `<관계>_<모드>.npz`로 저장하고(기본 asof는 기존 파일명 그대로 유지, 하위 호환), `05_train_gnn_condition_a.py`에는 `--sameday-graph-mode`로 어느 그래프 파일을 쓸지 선택하는 옵션을 추가했다.

### 검증

- 단위 테스트로 세 모드의 동작을 소규모 합성 데이터에서 직접 확인(같은 날 클러스터 크기별 엣지 수까지 손계산과 일치).
- **버그 발견·즉시 수정**: 첫 구현에서 bidirectional 모드가 같은 날 쌍에 대해 한쪽 방향만 추가하고 있었다(양방향이 아니었음). 단위 테스트로 바로 잡아냈다.
- 실제 데이터에 적용한 결과, **strict 모드에서 제거된 R-U-R 엣지 수(41,375개)가 9/12 보고서에 이미 계산돼 있던 "R-U-R 같은날 엣지 11.8%(41,375개)"와 정확히 일치** — 독립적으로 재현되어 구현 정확성을 다시 한번 확인했다.
- bidirectional 모드에서는 저활동형(첫 리뷰, type_new=1) 노드의 R-U-R 고립률이 100%→90.6%로 떨어진다(같은 날 쓴 두 번째 리뷰가 첫 리뷰에도 역방향 엣지를 보내므로) — 설계상 당연한 결과이며, 이 경우에는 06의 정합성 검사(1)·(2)를 모드별로 분기해 처리했다(같은 날 쌍만 미래 방향을 허용하도록 조정, 첫 리뷰 고립 검사는 asof/strict에서만 적용).

---

## 3. 실행 — 헤드라인 셀 민감도

**설정**: 비저활동 × R-U-R × GraphSAGE × scope=full × **시간 단위 분할**(주 결과, +0.050이 나온 바로 그 셀) × seed 5개(42,0~3). MLP 기준선은 그래프를 안 쓰므로 sameday_mode와 무관 — 하나만 새로 실행(--save-scores)해 세 비교에 공용으로 썼다.

| sameday_mode | Δ(GNN−MLP) | 95% CI(seed+test) | 판정 |
|---|---:|---|---|
| **asof**(기본, 헤드라인) | +0.0513 | [+0.0252, +0.0768] | ✅ 유의 |
| **strict**(같은 날 엣지 제거) | +0.0493 | [+0.0209, +0.0757] | ✅ 유의 |
| **bidirectional**(같은 날 양방향) | +0.0319 | [+0.0069, +0.0566] | ✅ 유의(하지만 하한이 0에 가까움) |

(asof 값 +0.0513은 MLP를 이번에 새로 재실행해서 나온 것이라 기존에 인용해온 +0.050 [+0.024,+0.076]과 정확히 같지는 않다 — 둘 다 오차범위 안에서 일치하며, MLP 재학습의 정상적인 변동폭이다.)

---

## 4. 해석

- **헤드라인 결과는 같은 날 엣지 처리 방식에 어느 정도 강건하다** — 세 모드 모두 유의하고, strict는 asof와 거의 같다(0.049 vs 0.051, −4%). 같은 날 엣지를 아예 없애도 결과가 크게 바뀌지 않는다는 것은, R-U-R의 신호가 "같은 날 애매한 순서" 덕분이 아니라 **날짜가 확실히 다른 진짜 과거 이력**에서 온다는 뜻이다 — §3(9/19 보고서)의 "정당한 과거 이력 효과" 해석과 같은 방향이다.
- **다만 bidirectional에서는 눈에 띄게 줄어든다**(0.051→0.032, −38%). 같은 날 쌍에 "미래" 정보(그날 나중에 쓴 리뷰)까지 양방향으로 섞어주면 오히려 신호가 흐려진다 — 이는 그래프에 불확실한 방향의 엣지를 더 넣는 것 자체가 노이즈로 작용할 수 있음을 보여준다(9/11 §9의 "관계를 합치면 손해" 패턴과 결이 비슷하다: 정보를 무조건 더 넣는다고 좋아지는 게 아니다).
- **결론**: 세 모드 모두 유의하므로 "헤드라인 결과가 같은 날 처리 방식의 인공물"이라는 우려는 기각된다. 다만 정확한 크기(+0.050 근방)는 "asof(현행)" 컨벤션에 다소 의존적이며, 더 보수적인 bidirectional 기준으로는 절반 가까이(+0.032) 줄어들 수 있다는 점을 한계로 명시할 가치가 있다.

---

## 5. 결과 파일

- `results/condition_a/phase1b/high_rur_sage_full_timesplit_sdg-{strict,bidirectional}{,_seed0..3}.json` (GNN, 10개)
- `results/mlp_baseline/high_all_seed{42,0,1,2,3}_timesplit.json` (MLP 시간단위 기준선, 이번에 처음 `--save-scores`로 재생성 — 5개)
- `results/bootstrap/sameday_sensitivity_{asof,strict,bidirectional}.json` (3개)
- `data/processed/graph_2014/{rur,rsr,rtr}_{strict,bidirectional}.npz` (그래프 파일, git 비대상)

## 6. 코드 변경

- `src/features/structural/temporal_graph.py`: `past_only_adjacency()`에 `sameday_mode` 파라미터 추가
- `scripts/06_build_graphs_2014.py`: `--sameday-mode` CLI, 모드별 파일명·정합성 검사 분기
- `scripts/05_train_gnn_condition_a.py`: `--sameday-graph-mode` CLI, `load_graph_2014()`가 모드별 파일을 찾아 로드

## 7. 남은 것

미팅자료 3부의 "9/14 이후 신규 목록"이 이제 다 처리됐다. 남은 항목은 우선순위가 낮다고 이미 표시된 것들뿐이다: `experiment_matrix.md`의 옛 서술 정리, 리뷰무작위·작성자단위 분할의 나머지 40개 셀 seed 반복. 그 외에는 여전히 **9/13에 준비한 교수님 질문 4가지에 대한 답변 대기**가 핵심 병목이다.
